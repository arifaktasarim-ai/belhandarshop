# Yerel geliştirme için:
NEXT_PUBLIC_API_URL="http://localhost:5000/api"

# Render.com'a deploy ederken bu değeri backend servisinin gerçek adresiyle değiştir, örn:
# NEXT_PUBLIC_API_URL="https://belhandar-backend.onrender.com/api"
# NOT: Bu değişken build sırasında koda gömülür (NEXT_PUBLIC_ öneki). Değiştirdikten sonra
# Render'da "Clear build cache & deploy" ile yeniden build almayı unutma.
